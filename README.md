# 🧩 TIE–Dialog Pilot Replication
### *Human–Machine Convergence in the Theory of Informational Emergence (TIE)*

This repository contains the full computational pipeline, datasets, and figure-generation scripts used in the paper:

> **Céspedes Jiménez, A. J. (2025). _Human–Machine Convergence in the TIE–Dialog Pilot Study._ University of Groningen — Faculty of Arts.**  
> DOI: [10.5281/zenodo.17516211](https://doi.org/10.5281/zenodo.17516211)

---

## 📘 Overview
The **TIE–Dialog pilot** presents the first empirical quantification of conversational coherence using the **Theory of Informational Emergence (TIE)**.

It compares human-annotated coherence events (ruptures and repairs) with automated detections generated by the TIE–Dialog model, computing alignment metrics such as **F1**, **κ**, and **Dynamic Time Warping (DTW)**.

Key results:
- Macro-scale alignment: `r_warped ≈ 0.93`
- Event-level F1: `≈ 0.70–0.88`
- Mean lag: `≈ +0.47 turns` (human-led synchronization)
- Proto-coherence signature: `≈ –0.3 turns` anticipation by model

---

## 🧠 Theoretical Context
Under the **Theory of Informational Emergence (TIE)**, conversational coherence arises from synchronization between:
- the **internal informational configuration** *(Iₛ)*, and  
- the **external/matrix configuration** *(Iₘ)*.

The TIE–Dialog framework models this coupling as a temporal coherence function:

C_t = f(sim(IS, IM), dt)


Coherence peaks (repairs) and valleys (ruptures) emerge as structural markers of shared understanding.

---

## ⚙️ Installation

### 1. Clone the repository
```bash
git clone https://github.com/AdolfoJCJ/tie-dialog-pilot-replication.git
cd tie-dialog-pilot-replication
```

### 2. Create and activate environment
**Option A — conda**
```bash
conda create -n tie-dialog-pilot python=3.11
conda activate tie-dialog-pilot
pip install -r requirements.txt
```

**Option B — venv**
```bash
python -m venv .venv
source .venv/bin/activate     # (Windows: .venv\Scripts\activate)
pip install -r requirements.txt
```

---

## 📂 Data setup

Download the pilot dataset and analysis scripts from Zenodo:

```bash
wget https://zenodo.org/records/17516211/files/tie-dialog-pilot-data.zip
unzip tie-dialog-pilot-data.zip -d data/raw/
```

Minimal CSV examples are also provided for quick testing in `data/sample/`.

---

## ▶️ Run the pipeline

Execute the full analysis:

```bash
python scripts/run_pipeline.py --config configs/config.sample.yml
```

or simply:

```bash
make all
```

Outputs:
- Coherence curves (`reports/figures/*.png`)
- Event metrics (`reports/artifacts/metrics_summary.csv`)
- DTW alignment tables (`reports/artifacts/dtw_results.csv`)
- Final report PDF (`reports/tie_dialog_report.pdf`)

If you encounter import issues, add this snippet to the top of `scripts/run_pipeline.py`:
```python
import sys, pathlib
sys.path.append(str(pathlib.Path(__file__).resolve().parents[1] / "src"))
```

---

## 🧪 Run tests

```bash
pytest -q
```

All tests should pass under Python 3.11.

---

## 🧮 Dependencies
Core libraries:

| Library | Version |
|----------|----------|
| Python | 3.11 |
| NumPy | 1.26 |
| SciPy | 1.13 |
| scikit-learn | 1.5 |
| sentence-transformers | 2.3 |
| matplotlib | 3.9 |
| fastdtw | 0.3.4 |
| pandas | 2.2 |

---

## 📊 Reproducing paper metrics

To reproduce the metrics in the paper:

```bash
python scripts/run_metrics_summary.py
```

This script will regenerate the aggregated F1, κ, and DTW tables reported in:
> **Appendix C — Data Tables**  
> of the paper *Human–Machine Convergence in the TIE–Dialog Pilot Study.*

---

## 🔬 Citation

If you use this repository, please cite:

```
Céspedes Jiménez, A. J. (2025).
Human–Machine Convergence in the TIE–Dialog Pilot Study (v1.0).
Zenodo. https://doi.org/10.5281/zenodo.17516211
```

---

## 🧩 Licensing

| Component | License | Notes |
|------------|----------|-------|
| Code (this repo) | MIT | Free for research use |
| Data & Figures | CC BY-NC 4.0 | Attribution–NonCommercial |
| Papers / PDFs | CC BY-NC 4.0 | Must cite author and source |

© 2025 Adolfo J. Céspedes Jiménez — University of Groningen  
Email: [a.j.cespedes.jimenez@student.rug.nl](mailto:a.j.cespedes.jimenez@student.rug.nl)

---

## 🌐 Related resources
- [Preprint (Zenodo)](https://doi.org/10.5281/zenodo.17516211)
- [Supplementary Appendices PDF](https://doi.org/10.5281/zenodo.17516211)

---

**Last updated:** November 2025  
*This repository is part of the ongoing development of the Theory of Informational Emergence (TIE) and its computational extensions.*
